import { Hero } from "@/components/home/hero"

/**
 * The homepage — docs/product.md §4.
 *
 * The neutral fallback someone reaches by trimming a persona code off a shared
 * link. It must look finished and reveal nothing.
 */
export default function HomePage() {
  return <Hero />
}
